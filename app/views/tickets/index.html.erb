<% if @tickets.size > 0 %>
    <%= render 'list', tickets: @tickets %>
<% else %>
    <div class="card">
        <div class="card-header">
            <h1>No Tickets Available</h1>
        </div>
    </div>
    <br>
<% end %>

<%= link_to 'Back', :back%>